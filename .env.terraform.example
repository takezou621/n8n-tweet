# Terraform Environment Variables Template
# Copy this file to .env.terraform and fill in your actual values
# IMPORTANT: Do not commit .env.terraform to version control

# Proxmox Provider Configuration
export TF_VAR_proxmox_api_url="https://your-proxmox-server.example.com:8006/api2/json"
export TF_VAR_proxmox_api_token_id="your-username@pam!your-token-id"
export TF_VAR_proxmox_api_token_secret="your-api-token-secret"

# AWS Backend Configuration
export TF_VAR_state_bucket_name="your-terraform-state-bucket"
export TF_VAR_state_bucket_region="us-east-1"
export TF_VAR_state_lock_table_name="terraform-state-lock"
export TF_VAR_state_kms_key_id="alias/terraform-state-key"
export TF_VAR_aws_profile="default"

# SSH Configuration
export TF_VAR_ssh_public_key_path="~/.ssh/id_rsa.pub"
export TF_VAR_ssh_private_key_path="~/.ssh/id_rsa"

# Usage Instructions:
# 1. Copy this file: cp .env.terraform.example .env.terraform
# 2. Fill in your actual values in .env.terraform
# 3. Source the file: source .env.terraform
# 4. Run Terraform: terraform plan

# Security Notes:
# - The .env.terraform file is automatically ignored by git
# - Use strong, unique credentials for each environment
# - Enable MFA on your accounts
# - Rotate credentials regularly