# npm security configuration
audit-level=moderate
fund=false

# Always run audit on install
audit=true

# Security settings
package-lock=true
save-exact=true

# Registry settings (use official npm registry for security)
registry=https://registry.npmjs.org/

# Disable automatic package updates during install
save=false
save-dev=false